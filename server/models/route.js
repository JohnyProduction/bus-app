class Route {
    constructor(id, carrierId, beginRouteStop, endRouteStop) {
      this.id = id;
      this.carrierId = carrierId;
      this.beginRouteStop = beginRouteStop;
      this.endRouteStop = endRouteStop;
    }
  }
  
  module.exports = Route;
  